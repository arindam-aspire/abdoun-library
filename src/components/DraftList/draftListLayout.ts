/** Layout helper on top of `Card` defaults (radius, shadow, `bg-card-background`). */
export const draftListCardClassName = "overflow-hidden";

export const draftListPaginationClassName = "mt-4 border-t-0 pt-0";
