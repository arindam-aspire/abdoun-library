/** Vertical stack spacing for property form steps. */
export const propertyFormStackClasses = "flex flex-col gap-2 md:gap-4";

/** Property form steps: one field per row below `md`; two columns from `md` up. */
export const propertyFormGridClasses =
  "grid grid-cols-1 gap-2 md:grid-cols-2 md:gap-4";

/** Full-width field/header within the property form grid. */
export const propertyFormGridSpanClasses = "col-span-1 md:col-span-2";
