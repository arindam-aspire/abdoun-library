/** Desktop and transposed header row surface. */
export const tableHeaderRowClassName = "bg-surface text-muted";

/** Shared header cell padding — taller from `sm` through `lg`. */
export const tableHeaderCellPaddingClassName =
  "px-3 py-3 sm:px-4 sm:py-4 lg:px-4 lg:py-4";

/** Pinned label column on mobile transposed layout (matches desktop header row). */
export const tablePinnedHeaderColumnClassName = "bg-surface";
