/** Horizontal dividers between body rows (desktop `sm` and up). */
export const tableRowDividerClassName = "border-b border-secondary/15";

/** Vertical dividers between columns (mobile transposed layout). */
export const tableColumnDividerClassName = "border-r border-secondary/15";

/** Vertical dividers between header columns from `md` up (not row dividers). */
export const tableDesktopHeaderColumnDividerClassName =
  "md:border-r md:border-secondary/15 last:md:border-r-0";

/** Divider below the table header row or above the pagination footer. */
export const tableSectionDividerClassName = "border-secondary/15";
